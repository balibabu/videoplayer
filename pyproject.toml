[project]
name = "mxplayer"
version = "0.1.0"
description = "Local video streaming server with MX Player style UI"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
    "python-dotenv>=1.0",
]
